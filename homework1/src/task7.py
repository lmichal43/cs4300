import numpy as numpy

#this function is meant to show the numpy package and some abilities we can do
def numpy_array(array):

    return {"this is the sum": numpy.sum(array), "this is the mean": numpy.mean(array), "this is the max": numpy.max(array),}